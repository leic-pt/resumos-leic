Tipo básicos:

- char
- int (32 bits)
- double
- float

Se quisermos um int de 64bits, podemos usar `long int`.

Sintaxe condicionais:

# `if-then-else`

```c
if (<expressao>) {
  <instrucao>
} else {
  <instrucao>
}
```
